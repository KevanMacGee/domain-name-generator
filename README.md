# SaaS App Name Generator

A small personal tool for turning a single word into different startup- and app-style names. Enter a base word, generate the suggestions, and copy the comma-separated list into a bulk domain search tool.
